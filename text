initialize a variable using boolean logic

select the button dom element

define a function 

use conditional logic to display  the color and 
change the text


use  isparty Time = !isparty time to switch the value
 
 partyButton,addevent listener (@)
 call the function
